services:
  - type: web
    name: dualscraper
    env: docker
    plan: free
    healthCheckPath: /api/status
    envVars:
      - key: NODE_ENV
        value: production
      - key: MEMORY_LIMIT_MB
        value: '450'
